      - name: Run Trend Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          # This command moves the computer into the right folder before running
          cd $GITHUB_WORKSPACE
          python .github/scripts/generate_post.py
