      - name: Run Trend Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          # This verifies where we are
          pwd
          ls -R .github/
          # This runs the script specifically
          python .github/scripts/generate_post.py
