      - name: Run Trend Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          echo "Listing all files in the current folder:"
          ls -R
          echo "Attempting to run script:"
          python .github/scripts/generate_post.py
